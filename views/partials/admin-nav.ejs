<%# Admin navigation now lives in the global sidebar (partials/header.ejs).
    This partial renders only the section eyebrow so existing includes keep working. %>
<div class="scr-eyebrow adm">// admin / <%= activeTab %></div>
